# Local development only. Points at the `db` service in docker-compose.yml:
#   docker compose --profile local up db -d
#
# Copy to .env and adjust if needed:
#   cp .env.example .env
#
# Production does not use this file — see "Segredos" in the README.
DATABASE_URL=postgresql://openar:openar@localhost:5432/openar
